import type { Metadata } from "next"; import { AlertCircle,ArrowRight,Scale } from "lucide-react"; import { ResourceCard } from "@/components/cards/resource-card"; import { Timeline } from "@/components/timeline"; import { resources } from "@/lib/data";
export const metadata:Metadata={title:"Perspectives",description:"Careful philosophical comparisons across physics, Buddhist thought, and consciousness."};
const comparisons=[{a:"Quantum relationality",b:"Dependent origination",body:"Both challenge descriptions based on isolated, self-sufficient entities. Physics describes measurable systems; dependent origination is a broader philosophical and soteriological framework."},{a:"Measurement",b:"Mindful observation",body:"A quantum measurement is a physical interaction formalized by theory. Mindfulness is a trained mode of attending to experience. Shared language around “observation” does not make them the same process."},{a:"Indeterminacy",b:"Impermanence",body:"Quantum probabilities constrain possible measurement outcomes. Impermanence describes conditioned phenomena as changing. The parallel can prompt reflection, but it does not establish identity."}];
export default function Perspectives(){return <><header className="page-hero"><p className="kicker">Comparative inquiry</p><h1 className="page-title mt-6">Resonance, with boundaries.</h1><p className="mt-8 max-w-2xl text-base leading-8 text-mist">Some ideas can be placed in conversation without being collapsed into one another. Here, similarities invite questions; differences protect understanding.</p></header><section className="shell pb-24"><div className="flex gap-4 rounded-lg border border-gold/30 bg-gold/[.06] p-6"><AlertCircle className="mt-1 shrink-0 text-gold" size={20}/><div><p className="text-sm font-semibold text-ivory">A necessary distinction</p><p className="mt-2 max-w-3xl text-sm leading-7 text-mist">This section explores philosophical similarities and differences. It does not claim that quantum physics proves Buddhism.</p></div></div></section><section className="shell pb-28"><div className="space-y-4">{comparisons.map(c=><article className="grid gap-6 rounded-lg border border-white/10 bg-white/[.025] p-6 md:grid-cols-[1fr_auto_1fr] md:items-center md:p-9" key={c.a}><div><span className="domain-label">Science</span><h2 className="mt-3 font-serif text-3xl">{c.a}</h2></div><Scale className="text-gold" size={22}/><div><span className="domain-label">Philosophical comparison</span><h2 className="mt-3 font-serif text-3xl">{c.b}</h2></div><p className="text-sm leading-7 text-mist md:col-span-3 md:max-w-4xl">{c.body}</p></article>)}</div></section><section className="border-y border-white/10 bg-[#081421] py-24"><div className="shell grid gap-16 lg:grid-cols-2"><div><p className="kicker">Our comparative method</p><h2 className="section-title mt-4">Curiosity needs guardrails.</h2><p className="mt-6 max-w-md text-sm leading-7 text-mist">Each perspective makes its evidential and interpretive status visible so readers can follow the reasoning, question it, and go deeper.</p></div><Timeline/></div></section><section className="shell py-24"><div className="mb-10 flex items-end justify-between"><h2 className="section-title">Read perspectives</h2><ArrowRight className="text-gold"/></div><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{resources.filter(r=>r.domain==="Perspective"||r.slug==="observer"||r.slug==="emptiness").map(r=><ResourceCard resource={r} key={r.slug}/>)}</div></section></>}
