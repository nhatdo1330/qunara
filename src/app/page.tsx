import Link from "next/link"; import { ArrowRight } from "lucide-react";
import { Hero } from "@/components/hero"; import { CollectionCard } from "@/components/cards/collection-card"; import { ResourceCard } from "@/components/cards/resource-card"; import { QuoteCard } from "@/components/quote-card"; import { Newsletter } from "@/components/newsletter"; import { Reveal } from "@/components/ui/reveal"; import { collections, resources } from "@/lib/data";
export default function Home(){return <><Hero/><section className="shell py-24"><Reveal><div className="mb-10 flex items-end justify-between"><div><p className="kicker">Curated pathways</p><h2 className="section-title mt-4">Featured collections</h2></div><Link href="/collections" className="hidden items-center gap-2 text-xs text-gold sm:flex">View all <ArrowRight size={14}/></Link></div></Reveal><div className="grid gap-5 lg:grid-cols-3">{collections.map((c,i)=><Reveal delay={i*.08} key={c.title}><CollectionCard collection={c}/></Reveal>)}</div></section><QuoteCard/><section id="latest" className="shell py-24"><Reveal><p className="kicker">Recently published</p><h2 className="section-title mb-10 mt-4">Latest additions</h2></Reveal><div className="grid gap-5 md:grid-cols-2 xl:grid-cols-4">{resources.slice(0,4).map((r,i)=><Reveal delay={i*.06} key={r.slug}><ResourceCard resource={r}/></Reveal>)}</div></section><Newsletter/></>}
