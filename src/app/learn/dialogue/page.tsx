import type { Metadata } from "next";
import { DialoguePath } from "./dialogue-path";
export const metadata:Metadata={title:"The Dialogue",description:"An intellectually honest dialogue between quantum physics and Buddhist philosophy."};
export default function Dialogue(){return <DialoguePath/>}
