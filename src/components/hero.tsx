"use client";
import Link from "next/link"; import { ArrowRight, BookOpen } from "lucide-react"; import { motion, useScroll, useTransform } from "framer-motion"; import { Artwork } from "./art/artwork";
export function Hero(){const {scrollY}=useScroll();const y=useTransform(scrollY,[0,700],[0,90]);return <section className="relative min-h-[820px] overflow-hidden border-b border-white/10 pt-16">
  <motion.div style={{y}} className="absolute inset-y-0 right-0 w-full opacity-70 lg:w-[67%]"><Artwork type="orbit" className="size-full object-cover"/></motion.div><div className="absolute inset-0 bg-gradient-to-r from-midnight via-midnight/85 to-midnight/10"/>
  <div aria-hidden="true" className="absolute inset-0 overflow-hidden">{[...Array(20)].map((_,i)=><motion.i key={i} className="absolute size-px rounded-full bg-gold" style={{left:`${8+(i*17)%88}%`,top:`${12+(i*23)%75}%`}} animate={{opacity:[.1,.65,.1],y:[0,-8,0]}} transition={{duration:5+i%4,repeat:Infinity,delay:i*.2}}/>)}</div>
  <div className="shell relative z-10 flex min-h-[754px] items-center py-20"><div className="max-w-3xl"><motion.p initial={{opacity:0,y:12}} animate={{opacity:1,y:0}} className="kicker">Science <span className="mx-2 text-white/30">•</span> Wisdom <span className="mx-2 text-white/30">•</span> Experience</motion.p><motion.h1 initial={{opacity:0,y:24}} animate={{opacity:1,y:0}} transition={{delay:.1,duration:.8}} className="mt-7 font-serif text-[clamp(5rem,12vw,10rem)] leading-[.72] text-ivory">QUNARA</motion.h1><motion.p initial={{opacity:0,y:18}} animate={{opacity:1,y:0}} transition={{delay:.2}} className="mt-10 max-w-xl text-base leading-8 text-mist sm:text-lg">Explore trusted knowledge about quantum physics, Buddhist philosophy, consciousness, and human understanding.</motion.p><motion.div initial={{opacity:0,y:18}} animate={{opacity:1,y:0}} transition={{delay:.3}} className="mt-9 flex flex-wrap gap-3"><Link className="button-primary" href="/explore">Explore <ArrowRight size={15}/></Link><Link className="button-secondary" href="#latest"><BookOpen size={15}/>Latest resources</Link></motion.div></div></div>
  <p className="absolute bottom-7 right-8 hidden max-w-xs border-l border-gold/60 pl-4 text-[10px] leading-5 text-mist md:block">Original abstract artwork: orbit, relation, and the open field of inquiry.</p>
</section>}
