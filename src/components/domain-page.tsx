import { CategoryCard } from "./cards/category-card"; import { ResourceCard } from "./cards/resource-card"; import { Reveal } from "./ui/reveal"; import { Resource } from "@/lib/types";
export function DomainPage({kicker,title,intro,categories,resources}:{kicker:string;title:string;intro:string;categories:string[];resources:Resource[]}){return <><header className="page-hero"><p className="kicker">{kicker}</p><h1 className="page-title mt-6">{title}</h1><p className="mt-8 max-w-2xl text-base leading-8 text-mist">{intro}</p></header><section className="shell pb-24"><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{categories.map((c,i)=><Reveal delay={(i%4)*.05} key={c}><CategoryCard title={c} index={i}/></Reveal>)}</div></section><section id="resources" className="border-t border-white/10 bg-[#081421] py-24"><div className="shell"><p className="kicker">Selected reading</p><h2 className="section-title mb-10 mt-4">Start with trusted sources</h2><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{resources.map(r=><ResourceCard resource={r} key={r.slug}/>)}</div></div></section></>}
