"use client"; import { Search, X } from "lucide-react";
export function SearchBar({value,onChange}:{value:string;onChange:(v:string)=>void}){return <label className="flex h-14 items-center gap-3 rounded-lg border border-white/15 bg-white/[.035] px-4 transition focus-within:border-gold/60"><Search size={19} className="text-gold"/><span className="sr-only">Search resources</span><input value={value} onChange={e=>onChange(e.target.value)} className="w-full bg-transparent text-sm text-ivory outline-none placeholder:text-mist/60" placeholder="Search topics, authors, or ideas…"/>{value&&<button onClick={()=>onChange("")} aria-label="Clear search"><X size={16}/></button>}</label>}
