import Link from "next/link"; import { ArrowRight } from "lucide-react"; import { Collection } from "@/lib/types"; import { Artwork } from "../art/artwork";
export function CollectionCard({collection}:{collection:Collection}){return <Link href={collection.href} className="group relative min-h-[420px] overflow-hidden rounded-lg border border-white/10"><Artwork type={collection.art} className="absolute inset-0 size-full object-cover transition duration-700 group-hover:scale-105"/><div className="absolute inset-0 bg-gradient-to-t from-midnight via-midnight/30 to-transparent"/><div className="absolute inset-x-0 bottom-0 p-7"><span className="domain-label">{collection.count} resources</span><h3 className="mb-3 mt-4 font-serif text-3xl text-ivory">{collection.title}</h3><p className="mb-5 max-w-sm text-sm leading-6 text-mist">{collection.description}</p><span className="inline-flex items-center gap-2 text-xs text-gold">Open collection <ArrowRight size={14}/></span></div></Link>}
