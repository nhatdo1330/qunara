import { CheckCircle2 } from "lucide-react";
export function Timeline(){const items=[["Question","Name the comparison precisely."],["Distinguish","Separate empirical claims from philosophical readings."],["Verify","Trace scientific claims to primary or expert sources."],["Interpret","Offer comparisons with explicit limits and alternatives."]];return <div className="relative border-l border-gold/30 pl-8">{items.map(([title,text],i)=><div className="relative mb-10 last:mb-0" key={title}><CheckCircle2 size={17} className="absolute -left-[41px] top-1 bg-midnight text-gold"/><span className="text-[10px] text-mist">0{i+1}</span><h3 className="mt-2 font-serif text-2xl">{title}</h3><p className="mt-2 text-sm leading-6 text-mist">{text}</p></div>)}</div>}
